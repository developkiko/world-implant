stages:
    - deploy

deploy:
    image: ruby:latest
    stage: deploy
    before_script:
        - gem install dpl
    scripts:
        - dpl --provider=heroku --app=$HEROKU_APP_NAME --api=HEROKU_API_KEY
    only:
        - main
